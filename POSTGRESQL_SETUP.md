# 🗄️ Настройка PostgreSQL для надёжного хранения данных

## Зачем PostgreSQL вместо SQLite?

### SQLite (текущий):
- ❌ Может терять данные при рестарте контейнера
- ❌ Нет автоматических backup
- ❌ Ограничен для production

### PostgreSQL (рекомендуется):
- ✅ Данные **никогда не теряются**
- ✅ Автоматические backup
- ✅ Production-ready
- ✅ Масштабируемость
- ✅ Бесплатно на Railway!

---

## 🚂 Настройка PostgreSQL на Railway

### Шаг 1: Добавить базу данных

1. В Railway открой свой проект
2. Нажми **"+ New"**
3. Выбери **"Database"** → **"Add PostgreSQL"**
4. Railway создаст PostgreSQL базу автоматически

### Шаг 2: Подключить к проекту

1. После создания PostgreSQL появится в списке сервисов
2. Railway **автоматически** создаст переменную `DATABASE_URL`
3. Бот **автоматически** подключится к PostgreSQL!

### Шаг 3: Проверить

1. Открой логи бота
2. Должно быть: `🗄️ Using PostgreSQL (production mode)`
3. Или: `✅ PostgreSQL database initialized and ready`

### Готово! ✅

Теперь все данные сохраняются в PostgreSQL и **никогда не теряются**!

---

## 🔄 Автоматическое переключение

Бот **автоматически** определяет какую базу использовать:

- **Локально (SQLite):** Если нет PostgreSQL URL
- **Облако (PostgreSQL):** Если Railway создал DATABASE_URL

Никаких дополнительных настроек не нужно!

---

## 📊 Что сохраняется в PostgreSQL

✅ Все пользователи  
✅ Все заметки  
✅ Все напоминания  
✅ План на день  
✅ Энергия (логи)  
✅ Вечерние чек-ины  
✅ Состояния пользователей  

**Всё надёжно и навсегда!**

---

## 🔍 Проверка что PostgreSQL работает

### В логах бота должно быть:
```
🗄️  Using PostgreSQL (production mode)
✅ PostgreSQL database initialized and ready
```

### В Railway:
1. Открой PostgreSQL сервис
2. Вкладка **"Data"** → можно увидеть таблицы
3. Вкладка **"Connect"** → информация о подключении

---

## 💾 Backup (автоматически)

Railway **автоматически** делает backup PostgreSQL:
- Ежедневные backup
- Хранятся 7 дней (на бесплатном плане)
- Можно восстановить одним кликом

---

## 🆓 Стоимость

**PostgreSQL на Railway - БЕСПЛАТНО!**

Входит в $5 кредитов:
- 256MB RAM
- 1GB диска (достаточно для тысяч пользователей)
- Автоматические backup

**Для бота этого более чем достаточно!**

---

## 🔧 Troubleshooting

### Бот не подключается к PostgreSQL:

1. **Проверь переменную DATABASE_URL:**
   - В Railway → Service → Variables
   - Должна быть создана автоматически

2. **Проверь логи:**
   - Если ошибка подключения, посмотри логи
   - Убедись что PostgreSQL сервис запущен (зелёный статус)

3. **Перезапусти бота:**
   - Railway → Deployments → Redeploy

### База данных пустая:

- Это нормально при первом запуске
- Таблицы создаются автоматически
- Данные появятся когда пользователи начнут использовать бота

---

## 📈 Масштабирование

Если бот вырастет (тысячи пользователей):

1. **Railway автоматически масштабирует** PostgreSQL
2. Можно увеличить план в один клик
3. Или мигрировать на внешний PostgreSQL (Render, Supabase)

**Но для начала бесплатного tier хватит!**

---

## ✅ Итог

После настройки PostgreSQL:
- ✅ Данные никогда не потеряются
- ✅ Автоматические backup
- ✅ Production-ready
- ✅ Бесплатно на Railway
- ✅ Автоматическое переключение (SQLite ↔ PostgreSQL)

**Настройка занимает 2 минуты и даёт полную надёжность!**

