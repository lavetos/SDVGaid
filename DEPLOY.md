# 🚀 Деплой бота в облако

Есть несколько способов запустить бота постоянно в облаке:

## Вариант 1: Railway.app (Рекомендуется) 🚂

**Бесплатно** до $5 кредитов в месяц

### Шаги:

1. **Зарегистрируйся на [railway.app](https://railway.app)**
   - Войди через GitHub

2. **Создай новый проект**
   - Click "New Project"
   - Выбери "Deploy from GitHub repo"
   - Выбери свой репозиторий `SDVGaid`

3. **Добавь переменные окружения**
   - В Settings → Variables добавь:
     ```
     BOT_TOKEN=твой_токен
     OPENAI_API_KEY=твой_ключ_или_оставь_пустым
     ANTHROPIC_API_KEY=твой_ключ_или_оставь_пустым
     DATABASE_URL=sqlite+aiosqlite:///adhd_bot.db
     ```

4. **Деплой автоматический**
   - Railway сам определит что это Python проект
   - Файл `railway.json` настроит деплой

### Railway использует:
- Автоматический деплой при push в GitHub
- Бесплатный SQLite (файловая база)
- Автоматический рестарт при падении

---

## Вариант 2: Render.com 🎨

**Бесплатно** с ограничениями (сон каждые 15 минут неактивности)

### Шаги:

1. **Зарегистрируйся на [render.com](https://render.com)**
   - Войди через GitHub

2. **Создай новый Web Service**
   - Click "New" → "Web Service"
   - Подключи GitHub репозиторий

3. **Настройки:**
   - **Name:** `sdvgaid-bot`
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python bot.py`
   - **Plan:** Free

4. **Добавь переменные окружения:**
   ```
   BOT_TOKEN
   OPENAI_API_KEY (опционально)
   ANTHROPIC_API_KEY (опционально)
   DATABASE_URL=sqlite+aiosqlite:///adhd_bot.db
   ```

5. **Создай базу данных (опционально):**
   - "New" → "PostgreSQL" (если хочешь persistent DB)
   - Или используй SQLite файл (данные могут потеряться при рестарте)

**⚠️ Внимание:** На бесплатном плане Render засыпает через 15 минут бездействия. Для постоянной работы нужен платный план ($7/месяц).

---

## Вариант 3: Fly.io 🪰

**Бесплатно** с ограничениями

### Шаги:

1. **Установи Fly CLI:**
   ```bash
   curl -L https://fly.io/install.sh | sh
   ```

2. **Войди:**
   ```bash
   fly auth login
   ```

3. **Создай приложение:**
   ```bash
   fly launch
   ```

4. **Добавь секреты:**
   ```bash
   fly secrets set BOT_TOKEN=твой_токен
   fly secrets set OPENAI_API_KEY=твой_ключ
   ```

5. **Деплой:**
   ```bash
   fly deploy
   ```

---

## Вариант 4: PythonAnywhere 🐍

**Бесплатно** для начинающих

### Шаги:

1. **Зарегистрируйся на [pythonanywhere.com](https://www.pythonanywhere.com)**

2. **Upload файлы:**
   - В Files загрузи все файлы проекта
   - Или используй Git

3. **Настрой Tasks:**
   - Tasks → Create new task
   - Command: `python3.9 bot.py`
   - Schedule: "Every day at 00:00" (или как хочешь)

4. **Добавь переменные окружения:**
   - Files → `.env` файл

**⚠️ Ограничения:** Бесплатный план позволяет запускать задачи по расписанию, но не постоянно.

---

## Рекомендация 🎯

**Для постоянной работы:** **Railway.app**
- Самый простой деплой
- Бесплатный tier щедрый
- Автоматический рестарт
- GitHub интеграция

**Для тестирования:** **Render.com** (free tier)
- Легко настроить
- Но засыпает через 15 минут

---

## Настройка после деплоя

После деплоя на любой платформе:

1. **Проверь логи:**
   - Убедись что бот запустился
   - Проверь что нет ошибок

2. **Протестируй бота:**
   - Отправь `/start` в Telegram
   - Проверь что бот отвечает

3. **Мониторинг:**
   - Следи за логами первые несколько дней
   - Проверь что бот не падает

---

## Troubleshooting

### Бот не отвечает:
- Проверь логи на ошибки
- Убедись что BOT_TOKEN правильный
- Проверь что приложение запущено

### База данных пустая:
- SQLite файл может сбрасываться при рестарте (на Render)
- Для persistent DB используй PostgreSQL (Railway)

### Бот падает:
- Проверь что все зависимости установлены
- Проверь логи на ошибки
- Убедись что API ключи правильные

---

## Стоимость

| Сервис | Бесплатный tier | Платный |
|--------|----------------|---------|
| Railway | $5 кредитов/месяц | $20+/месяц |
| Render | Free (сон 15 мин) | $7+/месяц |
| Fly.io | 3 shared VMs | $1.94+/месяц |
| PythonAnywhere | Ограничено | $5+/месяц |

**Для этого бота бесплатного tier достаточно!**

